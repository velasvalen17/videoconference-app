import React from "react";

export const Statistics = () => (
  <div>
    <h1>Statistics</h1>

    <p>
      This is where the calls statistics should go. For each call and for all
      the calls in the program?
    </p>
  </div>
);
